# 前端环境变量配置示例
# 复制此文件为 .env 并根据实际情况修改配置

# 文件服务基础URL (可选)
# 开发环境通常不需要设置，会自动使用 http://localhost:3000
# 生产环境可以设置为实际的服务器地址
# VITE_FILE_BASE_URL=https://your-domain.com

# PDF工作线程文件URL (可选)
# 开发环境通常不需要设置，会自动使用本地地址
# 生产环境可以设置为CDN或服务器地址
# VITE_PDF_WORKER_URL=https://your-cdn.com/static/JS/pdf.worker.min.js

# API接口基础URL (可选)
# 如果有需要可以设置API的基础地址
# VITE_API_BASE_URL=https://api.your-domain.com

# 开发环境配置
NODE_ENV=development